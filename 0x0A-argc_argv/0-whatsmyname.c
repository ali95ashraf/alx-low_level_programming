#include <stdio.h>

/**
 * main - Entry point
 * Description: A program that prints its name.
 * @argc: int
 * @argv: list
 * Return: Always 0 (Success)
 */

int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}

